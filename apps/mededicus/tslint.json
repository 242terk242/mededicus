{
  "extends": "../../tslint.json",
  "rules": {
    "directive-selector": [
      true,
      "attribute",
      "mededicus",
      "camelCase"
    ],
    "component-selector": [
      true,
      "element",
      "mededicus",
      "kebab-case"
    ]
  }
}
